создаем проект:
 создаем проект на python в котором будут функции для работы с генерацией текста, используя Chat Completions API. Важно, чтобы эта функция поддерживала режим диалога, чтобы нейросеть запоминала контекст и чат не прекращался.
мы подключаемся по апи к https://alltokens.ru
Все примеры в  документации  используют рабочий адрес https://api.alltokens.ru/api/v1
пример запроса: 
curl -X POST "https://api.alltokens.ru/api/v1/chat/completions" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "alltokens/auto",
    "messages": [
      {"role": "system", "content": "Отвечай кратко и по делу."},
      {"role": "user", "content": "Скажи привет в одном слове"}
    ]
  }'

в меню должен быть доступен выбор из следующих моделей:
cohere/north-mini-code:free
alltokens/pareto-code
minimax/minimax-m3
google/gemini-3-flash-preview
openai/gpt-oss-20b:free
anthropic/claude-haiku-4.5

названия моделей не менять!
лишние файлы не создавать 
экономить ответ для пользователя, только суть

Ты — опытный Python-разработчик. Твоя задача — написать код строго по предоставленному Техническому Заданию (ТЗ).

ЭКОНОМИЯ ТОКЕНОВ И РЕЖИМ ОТВЕТА (КРИТИЧЕСКИ ВАЖНО):
1. ЗАПРЕЩЕНО выводить любые вводные слова, приветствия, объяснения архитектуры, размышления ("Chain of Thought") или комментарии после кода.
2. ЗАПРЕЩЕНО писать текстовые пошаговые инструкции по запуску или установке библиотек.
3. Выводи ТОЛЬКО чистый, готовый к запуску Python-код внутри одного блока разметки ```python ```.
4. Каждый токен стоит денег — пиши лаконичный, эффективный код без лишнего дублирования и "воды". Не трать контекст на пустые рассуждения.
