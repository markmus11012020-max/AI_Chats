Задача:
создай отдельную вкдадку в интерфейсе для решения задачи написания Chains 
Создай  цепочку из 3–4 звеньев (этапов) для решения конкретной задачи — анализа данных

Опиши логику шагов и реализуй цепочку в коде на Python с помощью LangChain 

Минимальная структура цепочки:

Анализ задачи
Подбор инструментов
Реализация (генерация результата)
Проверка / ревью

Скрипт должен:

Принимать текстовое техническое описание задания.
Анализировать задание и выделять ключевые функции.
Автоматически генерировать Python-код.
Проверять код на корректность (минимально).
Возвращать готовый .py файл.
TECH STACK

langchain
python
openai  
STEPS (Цепочка должна состоять из 3 звеньев):

analysis_chain
Принимает текст задания и формирует структуру:
список команд
список обработчиков
формат ответа
нужна ли база данных
дополнительные зависимости
code_chain
На основе структуры генерирует полный код 
Требования к коду:

Проверяет код:
нет ли синтаксических ошибок
корректная структура запуска
корректные импорты
OUTPUT
Вернуть:

файл script.py с реализацией цепочки
пример запуска:
python script.py "Анализ данных по ставке ЦБ"
ADDITIONAL PROMPT RULES

Отвечай только кодом, без объяснений.
Код должен быть рабочим и запускаться без модификаций.
При необходимости добавляй комментарии в коде.
должны быть шаги анализ задания  → подбор инструментов для генерации → создание структуры кода → реализация кода